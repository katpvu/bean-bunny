import { useEffect } from "react";

const BusinessHours = ({hours}) => {

    const parseHours = () => {
        
    }
    useEffect(() => {
        console.log(hours)
    })
    return (
        <></>
    );
};

export default BusinessHours;